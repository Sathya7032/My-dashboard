package com.mydashboard.repo;

import com.mydashboard.entity.DebtInterest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DebtInterestRepository extends JpaRepository<DebtInterest, Long> {
}
